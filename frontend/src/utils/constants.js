export const ROLES = {
  ADMIN: 'ADMIN',
  CUSTOMER: 'CUSTOMER'
};

// API base URL is handled by Vite proxy in development (/api)
// But we keep this for consistency if needed in Axios/Fetch instances
export const API_BASE_URL = '/api';
